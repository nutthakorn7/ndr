apiVersion: v1
kind: Service
metadata:
  name: asset-service
  namespace: default
  labels:
    app: asset-service
spec:
  ports:
  - port: 8084
    name: http
  selector:
    app: asset-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asset-service
  namespace: default
spec:
  selector:
    matchLabels:
      app: asset-service
  replicas: 1
  template:
    metadata:
      labels:
        app: asset-service
    spec:
      containers:
      - name: asset-service
        image: ndr/rust-asset-service:latest
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: ndr-config
        env:
        - name: PORT
          value: "8084"
        ports:
        - containerPort: 8084
          name: http
        livenessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8084
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
