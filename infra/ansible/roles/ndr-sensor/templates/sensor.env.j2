SENSOR_ID={{ sensor_id | default(inventory_hostname) }}
TENANT_ID={{ tenant_id | default('default') }}
KAFKA_BROKERS={{ kafka_brokers | default('kafka:9092') }}
KAFKA_TOPIC={{ kafka_topic | default('zeek-logs') }}
CONTROLLER_URL={{ controller_url | default('') }}
CONTROLLER_TOKEN={{ controller_token | default('') }}
SENSOR_COMMAND_SECRET={{ sensor_command_secret | default('') }}
PCAP_RING_ENABLED={{ pcap_ring_enabled | default(true) }}
PCAP_EXPORT_DIR=/opt/ndr/pcap
