redef LogAscii::use_json = T;
redef LogAscii::json_timestamps = JSON::TS_ISO8601;
redef LogAscii::json_timestamps_precision = JSON::TS_MILLI;
redef LogAscii::include_meta = T;
redef Log::default_rotation_interval = 1hr;

@load tuning/json-logs
@load protocols/conn
@load protocols/dns
@load protocols/http
@load protocols/ssl
@load protocols/ftp
@load protocols/smb
@load frameworks/files/hash-all-files

redef capture_filters += {
  ["default"] = PacketFilter::default_capture_filter
};

redef Reporter::info_to_stderr = T;
